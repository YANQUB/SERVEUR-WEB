public class Main {
    public static void main(String[] args) {
        HttpServer server = new HttpServer(8080); // Utilisation du port port 8080
        server.start(); // Démarrez le serveur
    }
}
